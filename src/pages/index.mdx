import Navigator from '@/components/Navigator';
import Tag from '@/components/Tag';
import {MEMOIR, REACT_PROJECT} from '@/data.js';

<Tag>静态网站模板</Tag>
<div className='navigator-container'>
  {MEMOIR.map((item) => {
    return <Navigator key={item.id} item={item} />;
  })}
</div>

<Tag>React项目</Tag>
<div className='navigator-container'>
  {REACT_PROJECT.map((item) => {
    return <Navigator key={item.id} item={item} />;
  })}
</div>
